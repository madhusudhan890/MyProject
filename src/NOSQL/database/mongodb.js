const models = {};

// MODELS
models.User = require("../models/user.model");
models.Team = require("../models/team.model");
models.Notification = require("../models/notification.model");

module.exports = models;
